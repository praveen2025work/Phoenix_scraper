# Base URL of the headless SkillGap API. Optional — when unset, the SPA calls
# http://<page-hostname>:8000 (so LAN shares via IPv4 / hostname just work).
# Only set this if the API is on a different host/port than the page.
# VITE_API_BASE=http://127.0.0.1:8000

